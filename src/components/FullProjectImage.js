import React from "react";
import styled from "styled-components";

const FullProjectImageWrapper = styled.div``;

const FullProjectImage = () => {
  return <FullProjectImageWrapper></FullProjectImageWrapper>;
};

export default FullProjectImage;
